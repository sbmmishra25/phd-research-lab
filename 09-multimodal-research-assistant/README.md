# 09 — Multimodal Vision-Language Research Assistant

Lightweight image-text fusion baseline.

Image features + text features → normalized concatenation → downstream model.

Replace the simple features with a pretrained VLM for serious experiments. Study modality ablation, missing modalities, retrieval and grounding.
